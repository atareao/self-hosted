version: "3.7"

services:
  memos:
    image: neosmemo/memos:latest
    container_name: memos
    init: true
    restart: unless-stopped
    volumes:
      - memos:/var/opt/memos

volumes:
  memos: {}
